# Date: 2026-07-06

- **What I did:**
  Задание было проанализиолвать код напарника:

  Поработал над архитектурой каталога и корзины, чтобы всё работало гибко и было удобно тестировать. Вынес общие настройки в InjectionToken, что дало возможность легко переключаться между реальным API и заглушками — теперь через useMockData можно удобно проверять UI, не нагружая сеть. В карточке товара перешел на современные сигналы и OnPush для оптимизации рендеринга, а в самом сервисе ProductApi добавил логику перехвата запросов, чтобы при включенном моке сразу отдавать заготовленные данные из mockProduct.ts. В логике корзины всё реализовал через понятные методы управления количеством, которые напрямую работают со стейтом сервиса, поэтому шаблон получился чистым и предсказуемым. Также добавил loadingInterceptor для автоматического управления состоянием загрузки при запросах, который теперь работает в связке с LoadingService. Для всего этого покрытия написал тесты: проверил работу формы создания продукта, чтобы валидация полей и навигация срабатывали как надо, и отдельно прогнал тесты для LoadingService, чтобы убедиться, что индикатор загрузки корректно переключается при выполнении запросов.