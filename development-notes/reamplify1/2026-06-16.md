# Date: 2026-05-14

- **What I did:**
Continued work on the "67-shop" project. I created a new feature branch feat/price-format-pipe to implement custom formatting logic. Developed a custom PricePipe to handle product price display, utilizing toLocaleString('ru-RU') for automatic thousands-separator formatting. Integrated the pipe into the product list component to ensure consistent and clean currency display across the store.

- **Problems:**
Initially, I encountered a confusion regarding the PipeTransform interface and the correct way to handle optional or null values within the pipe. Also, I was initially unsure about how the toLocaleString method handles different locales and how to set a default currency value dynamically.

- **Solutions (or attempts):**
I implemented the PipeTransform interface and added logic to handle null | undefined values, returning a default "0" string to prevent runtime errors. I studied the MDN documentation for toLocaleString('ru-RU') to understand how it automatically handles number formatting according to regional standards. I set the default currency parameter to '$' directly in the transform method signature to satisfy the project's requirements for flexibility.

- **Thoughts / plans:**
This sprint has really improved my understanding of Angular's template layer. Creating a custom pipe showed me how powerful and clean data transformation can be when extracted from the component logic. My next goal is to implement a custom directive to add specific behavior to UI elements (like a hover effect or input masking) to further enhance user interaction in the shop. After that, I will move on to Reactive Forms and OnPush optimization.

- **Time spent:**: 2.5 hours
